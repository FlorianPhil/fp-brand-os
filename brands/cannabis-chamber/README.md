# Cannabis Chamber Brand OS Team-Review

This directory is a GitHub Pages publish target for the Cannabis Chamber Brand OS team-review.

Do not edit generated HTML here as source. Make source changes in the Brand OS builder workspace, rebuild, package the review surface, then copy the review package into the deploy target.

Expected live entrypoint depends on the deploy path.
